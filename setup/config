# Something in the sudoers file
%andres>ALL=(ALL) NOPASSWD: /usr/sbin/pm-suspend

# Suspend on lid close
vi /etc/acpi/lid.sh
# Replace: . /usr/share/acpi-support/screenblank
#    with: echo -n mem > /sys/power/state
